{
  "name": "supply-chain-smart-contract-dapp",
  "version": "1.0.0",
  "description": "Solidity supply-chain dApp prototype for asset tracking, participant management, ownership history and token-based workflows.",
  "scripts": {
    "compile": "truffle compile",
    "test": "truffle test",
    "migrate": "truffle migrate",
    "migrate:local": "truffle migrate --network development --reset",
    "ganache": "ganache --port 7545 --host 127.0.0.1"
  },
  "devDependencies": {
    "@truffle/hdwallet-provider": "^2.1.15",
    "dotenv": "^16.4.5",
    "ganache": "^7.9.2",
    "truffle": "^5.11.5"
  }
}
